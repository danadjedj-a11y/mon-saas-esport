/**
 * APP.JSX SIMPLIFIÉ POUR TESTER CLERK + CONVEX
 * 
 * Cette version remplace temporairement l'App.jsx complexe
 * Une fois que Clerk fonctionne, on migrera progressivement les routes
 */

import TestAuth from './TestAuth';

export default function App() {
    return <TestAuth />;
}
